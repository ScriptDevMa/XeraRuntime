-- 北京时间 + FPS 显示（右上角清晰版）
do
    local gui = Instance.new("ScreenGui")
    gui.Name = "InfoDisplay"
    gui.Parent = game.CoreGui
    gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

    local label = Instance.new("TextLabel")
    label.Parent = gui
    label.BackgroundTransparency = 0.3
    label.BackgroundColor3 = Color3.new(0,0,0)
    label.Position = UDim2.new(0.7, 0, 0.02, 0)
    label.Size = UDim2.new(0, 320, 0, 36)
    label.Font = Enum.Font.GothamBold
    label.TextColor3 = Color3.new(1,1,1)
    label.TextStrokeColor3 = Color3.new(0,0,0)
    label.TextStrokeTransparency = 0
    label.TextSize = 18
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.TextYAlignment = Enum.TextYAlignment.Center

    local lastTime = tick()
    local frames = 0
    local fps = 0

    game:GetService("RunService").RenderStepped:Connect(function()
        frames = frames + 1
        local now = tick()
        if now - lastTime >= 1 then
            fps = frames
            frames = 0
            lastTime = now
        end
        local time = os.date("*t", now + 8*3600)
        label.Text = string.format("北京时间: %02d:%02d:%02d | FPS: %d", time.hour, time.min, time.sec, fps)
    end)
end

local UserInputService = game:GetService("UserInputService")
local function MakeDraggable(topbar, obj)
    local dragging = false
    local dragStart = nil
    local startPos = nil
    topbar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = obj.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType.Touch) then
            local delta = input.Position - dragStart
            obj.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

local function loadAllUI()
    local repo = 'https://raw.githubusercontent.com/KingScriptAE/No-sirve-nada./refs/heads/main/'
    local Library = loadstring(game:HttpGet(repo .. 'Library.lua'))()
    local ThemeManager = loadstring(game:HttpGet(repo .. 'addons/ThemeManager.lua'))()
    local SaveManager = loadstring(game:HttpGet(repo .. 'addons/SaveManager.lua'))()
    local Options = Library.Options

    local Window = Library:CreateWindow({
        Title = "虎虎恶魔整合脚本",
        Footer = "游戏辅助",
        Icon = 131153193945220,
        NotifySide = "Right",
        ShowCustomCursor = true,
        AutoShow = true,
    })

    local Tabs = {
        Games = Window:AddTab("游戏脚本", "gamepad"),
        Author = Window:AddTab("作者", "user"),
        Update = Window:AddTab("脚本更新", "update"),
        Settings = Window:AddTab("设置", "settings"),
    }

    local LeftGroup = Tabs.Games:AddLeftGroupbox("通用功能")
    local RightGroup = Tabs.Games:AddRightGroupbox("辅助功能")

    LeftGroup:AddButton({
        Text = "无限跳跃",
        Func = function()
            game.Players.LocalPlayer.Character.Humanoid.JumpPower = 100
            Library:Notify("无限跳跃已开启", 2)
        end,
    })
    LeftGroup:AddButton({
        Text = "无限体力",
        Func = function()
            game.Players.LocalPlayer.Character.Humanoid.Stamina = math.huge
            Library:Notify("无限体力已开启", 2)
        end,
    })
    LeftGroup:AddButton({
        Text = "角色加速",
        Func = function()
            game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 50
            Library:Notify("角色加速已开启", 2)
        end,
    })
    LeftGroup:AddButton({
        Text = "角色变大",
        Func = function()
            game.Players.LocalPlayer.Character.Humanoid.BodyDepthScale.Value = 3
            game.Players.LocalPlayer.Character.Humanoid.BodyHeightScale.Value = 3
            game.Players.LocalPlayer.Character.Humanoid.BodyWidthScale.Value = 3
            Library:Notify("角色变大已开启", 2)
        end,
    })

    RightGroup:AddButton({
        Text = "显示玩家名字",
        Func = function()
            for _,v in pairs(game.Players:GetPlayers()) do
                if v.Character then
                    local bill = Instance.new("BillboardGui")
                    bill.Parent = v.Character.Head
                    bill.Size = UDim2.new(0,200,0,50)
                    bill.Adornee = v.Character.Head
                    local text = Instance.new("TextLabel")
                    text.Parent = bill
                    text.BackgroundTransparency = 1
                    text.Size = UDim2.new(1,0,1,0)
                    text.Text = v.Name
                    text.TextColor3 = Color3.new(1,1,1)
                    text.TextScaled = true
                end
            end
            Library:Notify("已显示所有玩家名字", 2)
        end,
    })
    RightGroup:AddButton({
        Text = "高亮敌人",
        Func = function()
            for _,v in pairs(game.Players:GetPlayers()) do
                if v ~= game.Players.LocalPlayer and v.Character then
                    for _,p in pairs(v.Character:GetChildren()) do
                        if p:IsA("Part") or p:IsA("MeshPart") then
                            p.Material = Enum.Material.Neon
                            p.Color = Color3.new(1,0,0)
                        end
                    end
                end
            end
            Library:Notify("已高亮所有敌人", 2)
        end,
    })
    RightGroup:AddButton({
        Text = "恢复默认",
        Func = function()
            game.Players.LocalPlayer.Character.Humanoid.JumpPower = 50
            game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 16
            game.Players.LocalPlayer.Character.Humanoid.BodyDepthScale.Value = 1
            game.Players.LocalPlayer.Character.Humanoid.BodyHeightScale.Value = 1
            game.Players.LocalPlayer.Character.Humanoid.BodyWidthScale.Value = 1
            Library:Notify("已恢复默认设置", 2)
        end,
    })

    local AuthorGroup = Tabs.Author:AddLeftGroupbox("作者")
    AuthorGroup:AddLabel("虎虎大魔王")
    AuthorGroup:AddLabel("B站：FreddyXiaozuo")
    AuthorGroup:AddLabel("QQ群：705383133")
    AuthorGroup:AddButton({ Text = "复制QQ群", Func = function() setclipboard("705383133"); Library:Notify("已复制", 2) end })
    AuthorGroup:AddButton({ Text = "复制B站链接", Func = function() setclipboard("https://b23.tv/0QenU2x"); Library:Notify("已复制", 2) end })

    local NoticeGroup = Tabs.Author:AddRightGroupbox("公告")
    NoticeGroup:AddLabel("• 脚本持续更新中")
    NoticeGroup:AddLabel("• 如有问题请加群反馈")
    NoticeGroup:AddLabel("• 禁止倒卖与传播")
    NoticeGroup:AddLabel("• 免费使用，感谢支持")

    local UpdateGroup = Tabs.Update:AddLeftGroupbox("脚本更新日志")
    UpdateGroup:AddLabel("V1.0 - 初始版本")
    UpdateGroup:AddLabel("V1.1 - 新增通用功能")
    UpdateGroup:AddLabel("V1.2 - 优化UI界面")
    UpdateGroup:AddLabel("V1.3 - 修复时间显示问题")
    UpdateGroup:AddLabel("V1.4 - 新增脚本更新页")
    UpdateGroup:AddLabel("V1.5 - 移除广告与多余弹窗")

    local UpdateRight = Tabs.Update:AddRightGroupbox("更新说明")
    UpdateRight:AddLabel("每次更新会新增功能")
    UpdateRight:AddLabel("修复已知BUG")
    UpdateRight:AddLabel("优化运行流畅度")
    UpdateRight:AddLabel("适配更多游戏环境")

    local MenuGroup = Tabs.Settings:AddLeftGroupbox("菜单")
    MenuGroup:AddButton("卸载脚本", function() Library:Unload() end)
    MenuGroup:AddLabel("开关快捷键"):AddKeyPicker('MenuKey', { Default = 'RightShift', NoUI = true, Text = '快捷键' })
    Library.ToggleKeybind = Options.MenuKey

    ThemeManager:SetLibrary(Library)
    SaveManager:SetLibrary(Library)
    SaveManager:IgnoreThemeSettings()
    ThemeManager:SetFolder("HuHuTheme")
    SaveManager:SetFolder("HuHuConfig")
    SaveManager:BuildConfigSection(Tabs.Settings)
    ThemeManager:ApplyToTab(Tabs.Settings)
end

local keyGui = Instance.new("ScreenGui")
local mainFrame = Instance.new("Frame")
local titleLabel = Instance.new("TextLabel")
local lineFrame = Instance.new("Frame")
local keyInput = Instance.new("TextBox")
local verifyBtn = Instance.new("TextButton")

keyGui.Name = "KeySystem"
keyGui.Parent = game.CoreGui
keyGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

mainFrame.Parent = keyGui
mainFrame.Active = true
mainFrame.BackgroundColor3 = Color3.new(0.1,0.1,0.1)
mainFrame.BackgroundTransparency = 0
mainFrame.BorderSizePixel = 2
mainFrame.BorderColor3 = Color3.new(0,0,1)
mainFrame.Position = UDim2.new(0.5,-150,0.8,-67)
mainFrame.Size = UDim2.new(0,280,0,260)
mainFrame.Draggable = true
mainFrame.ClipsDescendants = true

local corner = Instance.new("UICorner")
corner.Parent = mainFrame
corner.CornerRadius = UDim.new(0,12)

titleLabel.Parent = mainFrame
titleLabel.BackgroundTransparency = 1
titleLabel.Position = UDim2.new(0,0,0.05,0)
titleLabel.Size = UDim2.new(1,0,0,60)
titleLabel.Font = Enum.Font.GothamBold
titleLabel.Text = "密钥验证"
titleLabel.TextColor3 = Color3.new(1,1,1)
titleLabel.TextScaled = true
titleLabel.TextStrokeColor3 = Color3.new(0,0,1)
titleLabel.TextStrokeTransparency = 0

lineFrame.Parent = titleLabel
lineFrame.BackgroundColor3 = Color3.new(0,0,1)
lineFrame.BorderSizePixel = 0
lineFrame.Position = UDim2.new(0.1,0,0.9,0)
lineFrame.Size = UDim2.new(0.8,0,0,4)

local inputCorner = Instance.new("UICorner")
keyInput.Parent = mainFrame
keyInput.BackgroundColor3 = Color3.new(0.2,0.2,0.2)
keyInput.BorderSizePixel = 0
keyInput.Position = UDim2.new(0.1,0,0.35,0)
keyInput.Size = UDim2.new(0.8,0,0,60)
keyInput.Font = Enum.Font.GothamSemibold
keyInput.PlaceholderText = "请输入卡密"
keyInput.Text = ""
keyInput.TextColor3 = Color3.new(1,1,1)
keyInput.PlaceholderColor3 = Color3.new(0.6,0.6,0.6)
keyInput.TextScaled = true
keyInput.ClearTextOnFocus = false
inputCorner.Parent = keyInput
inputCorner.CornerRadius = UDim.new(0,8)

local btnCorner = Instance.new("UICorner")
verifyBtn.Parent = mainFrame
verifyBtn.BackgroundColor3 = Color3.new(0,0,1)
verifyBtn.BackgroundTransparency = 0
verifyBtn.BorderSizePixel = 0
verifyBtn.Position = UDim2.new(0.2,0,0.7,0)
verifyBtn.Size = UDim2.new(0.6,0,0,55)
verifyBtn.Font = Enum.Font.GothamBold
verifyBtn.Text = "验证卡密"
verifyBtn.TextColor3 = Color3.new(1,1,1)
verifyBtn.TextScaled = true
btnCorner.Parent = verifyBtn
btnCorner.CornerRadius = UDim.new(0,8)

local CORRECT_KEY = "你的正确卡密"
local UI_LOADED = false

verifyBtn.MouseButton1Click:Connect(function()
    if keyInput.Text == CORRECT_KEY then
        keyGui:Destroy()
        if not UI_LOADED then
            UI_LOADED = true
            loadAllUI()
        end
    else
        game.Players.LocalPlayer:Kick("卡密错误！")
    end
end)
